# Fusi³ App Icon

Place your icon files here with one of the following names:

- `fusi3_icon.ico` (recommended for Windows EXE and taskbar/window icon)
- `fusi3_icon.png` (fallback)

The application and `build_fusi3.bat` will automatically use these files.
